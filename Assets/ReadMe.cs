

    //Les images dite SVG (image vectoriel) sont des images qui ne sont jamais pixaliser
    //Pour les importer dans la scene il faut soit les drag & drop, soit modifier le sprite renderer (le sprite en lui même, et le shader en Unlit_Vectorial Shader)
